language: rust
sudo: false

cache: cargo

os:
  - linux
  - osx

rust:
  - stable
  - nightly

env:
  - VERSION="stable"
  - VERSION="nightly"

matrix:
  fast_finish: true
  include:
    - rust: stable
      env: VERSION="stable"
    - rust: nightly
      env: VERSION="nightly"

script:
  - ./build.sh
