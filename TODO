 - Change columns() to return usize?
 - Vi cursor position doesn't reset after cancel
 - Vi-less search selection shifting after search is confirmed
 - Topmost/Bottommost line functions on Dimensions?
    -> Check how often (-history_size) and (screen_lines - 1) is used
 - URLs don't work when scrolled up
    -> Indexing used for RenderableCells is a bit funkyfunk, probably why
